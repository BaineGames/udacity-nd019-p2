import React, { Component } from "react";
import { connect } from "react-redux";

class Ask extends Component {


    render() {
        
        return (
            <div>ask</div>
        )
    }
}

export default (Ask);